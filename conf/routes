# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /                controllers.BrowserTarget.index
GET         /ws              controllers.BrowserTarget.socket
GET         /api/up          controllers.TargetResults.up(channel_name, user_id, user_name, text)
POST        /api/hit         controllers.TargetResults.hit
POST        /api/miss        controllers.TargetResults.miss
GET         /api/ranking     controllers.TargetResults.ranking
GET         /api/stats       controllers.TargetResults.stats(text)

GET         /assets/*file    controllers.Assets.at(path="/public", file)
GET         /webjars/*file   controllers.WebJarAssets.at(file)


